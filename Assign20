def FactSum(N): 
    f = 1
    Sum = 0
    for i in range(1, N + 1): 
         f = f * i 
         Sum += f 
    return Sum

n=int(input('enter n : '))

print('The sum of',n,'factorials is', FactSum(n))
